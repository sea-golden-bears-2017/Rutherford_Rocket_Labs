<!-- new part order form partial -->

<%= f.label 'Part Number' %>
<%= f.select(:part_id) do %>
  <% Part.all.each do |p| %>
  <%= content_tag(:option, p.part_no, value: p.id) %>
  <% end %>
<% end %>
<%= f.label 'Quantity' %>
<%= f.text_field :quantity, class: 'part-quantity' %>
